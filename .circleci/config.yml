version: 2
jobs:
  build:
    docker:
    - image: circleci/elixir:1.6.5
    working_directory: ~/99-Elixir-Problems
    steps:
      - checkout
      - run:
          name: "Run test(s)"
          command: mix deps.get && mix test

workflows:
  version: 2
  full_cycle:
    jobs:
      - build
